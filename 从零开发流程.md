# 个人笔记复习助手：从零开发流程

> 当前在线问答有两条清晰边界：**快速模式是单 Agent + 检索工具**；**精确查找是固定 Step RAG**。

## Agentic RAG 增补：问答 Agent、工具调用与耗时

### 快速模式短期记忆（v1.2）

1. 新建 `backend/app/services/conversation_memory.py`，按浏览器 `conversation_id` 管理纯内存会话。
2. 每个会话保留最近 3 轮原始问答；超过 3 轮后，将最早内容压缩为摘要。
3. 快速模式将“摘要 + 最近 3 轮 + 当前问题”传入 Agent，用于理解“我叫什么”“刚才那个”等追问。
4. 摘要任务在回答发送后异步执行，不额外拖慢当前回复；后端重启或新建浏览器会话时，记忆清空。
5. 精确查找不读、不写会话记忆，仍然是无状态的固定 Step RAG。

你要能说：这不是长期记忆，也不是数据库持久化；它是服务进程中的短期会话记忆，使用摘要减少 Token 和延迟。

1. 新建 `backend/app/services/agent_service.py`，定义 `SearchPersonalNotes` 工具。
2. 快速模式用 `chat_model.bind_tools()` 让 Agent 判断本轮是否需要调用检索工具；不要再用“你好/你是谁”等关键词 `if` 硬编码分流。
3. 快速模式中，Agent 不调用工具时直接输出基础问答；调用工具时只执行向量检索。
4. 精确查找不经过 Agent，固定执行查询改写、双路召回、RRF、精排和生成。
5. 把快速模式检索出的真实 Chunk 作为 `ToolMessage` 回传给 Agent，要求它只依据工具返回的资料回答。
6. `api/chat.py` 使用 `perf_counter()` 统计从接收请求到流式回答结束的总耗时，并通过 SSE 的 `done` 事件返回 `elapsed_ms`。
7. `frontend/app.py` 在每条助手消息下展示“本次回答耗时：x.xx 秒”。
8. 精确查找在首次使用前检查 Cross-Encoder 本机缓存：有缓存则提示“正在加载”，没有才提示“正在下载到本机缓存”；前端接收 SSE `stage` 事件显示进度条。
9. 笔记列表刷新失败不能直接当作空列表：前端需要短暂重试，并把“真实空库”和“后端暂时不可用”分别提示。

你要能说：这里不是多 Agent，也不是让模型随意规划；快速模式是一个单 Agent 的工具调用循环，Agent 负责“是否查、查什么”。精确查找则是可解释、可复现的固定 Step RAG 链路。

> 原则：一次只完成一个小文件；每一步都能运行验证后，再进入下一步。前端由 Codex 负责。

## 先区分两个顺序

### 开发验证顺序

先单独跑通“原问题 → 纯向量检索”作为基线，确认入库、Embedding 和 Chroma 没有问题；然后才加入查询改写、BM25、RRF 和精排。这样某一步结果变差时，能定位是哪一层造成的。

### 最终线上调用顺序

```text
原问题
→ 查询改写（失败时回退原问题）
→ 向量检索 + BM25
→ RRF 融合
→ Cross-Encoder 精排
→ 使用原问题 + 最终片段生成带来源回答
```

**结论**：纯向量检索是开发阶段的基线；真正给用户服务时，查询改写必须在检索之前执行。

## 0. 现在已有的环境

- `backend/app/main.py`：FastAPI 启动入口与健康检查。
- `frontend/app.py`：Streamlit 前端空壳，后续由 Codex 完成。
- 双击 `启动项目.cmd` 可以启动前后端。

当前无需修改 `main.py`。先从 `services` 里的纯 Python 文件开始。

## 1. 读取笔记

**新建文件**：`backend/app/services/note_loader.py`

**只做什么**：接收一个 Markdown 文件路径，以 UTF-8 读取并返回完整文本。

**不做什么**：不切分、不调用模型、不写接口。

**验证**：传入一份自己的 `.md` 笔记，打印前 100 个字符，确认中文没有乱码。

**你要能说**：这一层只负责输入，不负责 RAG；这样后面替换 PDF 解析不会影响检索逻辑。

---

## 2. 按标题切分笔记

**新建文件**：`backend/app/services/note_splitter.py`

**只做什么**：调用 `MarkdownHeaderTextSplitter`，把笔记切成多个 `Document`；每个片段保留文件名、标题路径、内容和稳定 `chunk_id`。

**稳定 `chunk_id`**：按“来源文件路径 + 本次切分序号”生成；同一份未改动笔记重建时应得到相同 ID。后续 RRF 需要用它判断向量结果和 BM25 结果是否是同一 Chunk。

**验证**：打印片段数量，以及任意 3 个片段的 `chunk_id`、标题和内容预览。

**你要能说**：优先保留 Markdown 标题结构，避免把同一概念的定义和解释硬切开。

---

## 3. 定义笔记数据格式

**新建文件**：`backend/app/schemas/note.py`

**只做什么**：定义后端输入输出模型。字段是接口契约，后续 API 与前端都按此结构通信。

### `ImportResult`：导入一份笔记后的结果

| 字段 | Python 类型 | 必填 | 用途 |
| --- | --- | --- | --- |
| `file_name` | `str` | 是 | 成功或失败的原始文件名 |
| `chunk_count` | `int` | 是 | 本次切分后写入的 Chunk 数量；失败时为 `0` |
| `status` | `Literal["success", "failed"]` | 是 | 导入最终状态，避免随意传字符串 |
| `error_message` | `str \| None` | 否 | 失败原因；成功时为 `None` |

### `NoteSummary`：笔记列表的一项

| 字段 | Python 类型 | 必填 | 用途 |
| --- | --- | --- | --- |
| `note_id` | `str` | 是 | 系统内笔记唯一标识，后续删除/重建索引时使用 |
| `file_name` | `str` | 是 | 前端展示的笔记文件名 |
| `chunk_count` | `int` | 是 | 该笔记当前包含的 Chunk 数量 |

### `SourceChunk`：回答附带的一条来源

| 字段 | Python 类型 | 必填 | 用途 |
| --- | --- | --- | --- |
| `chunk_id` | `str` | 是 | 命中的稳定片段标识，用于追溯 |
| `file_name` | `str` | 是 | 来源文件名 |
| `header_path` | `list[str]` | 是 | 标题层级，例如 `["项目开发计划", "目标"]` |
| `content_preview` | `str` | 是 | 在前端来源卡片中展示的原文预览 |

**验证**：在 Python 中创建一个对象并输出 JSON。

**你要能说**：这相当于 Spring Boot 的 DTO，接口层和业务层不直接传随意的字典。

---

## 3.1 统一环境配置

**新建文件**：`backend/app/core/config.py`

**只做什么**：在项目启动后统一加载根目录 `.env`，提供 API Key 与 Chroma 持久化目录；其他模块只能导入配置，不自行调用 `load_dotenv()`。

**验证**：打印 Chroma 数据目录，并只打印 API Key 是否存在，绝不打印真实 Key。

---

## 3.2 统一日志工具

**新建文件**：`backend/app/core/logger.py`

**只做什么**：封装 Python 标准库 `logging`，统一输出格式、控制台日志和 `logs/app.log` 文件日志。

**调用方式**：

```python
from backend.app.core.logger import get_logger

logger = get_logger(__name__)
logger.info("开始导入笔记")
logger.warning("笔记内容为空")
logger.error("导入失败")
```

**验证**：运行一次 `logger.info()` 后，控制台和 `logs/app.log` 都有同一条记录。

---

## 4. 建立向量知识库（普通 RAG 基线）

**新建文件**：`backend/app/storage/vector_store.py`

**只做什么**：用 DashScope Embeddings 将第 2 步的片段写入本地 Chroma；支持按笔记重新建立索引。

**验证**：导入笔记后，打印 Chroma 中的片段数量。

**你要能说**：Embedding 把文本变成向量，Chroma 负责保存向量和元数据，方便按语义查找相近片段。

---

## 5. 完成纯向量检索（仅作开发基线）

**新建文件**：`backend/app/services/vector_retriever.py`

**只做什么**：直接输入原问题，返回 Chroma 相似度最高的 Top-K 来源片段与距离。这个文件是底层能力，不是最终页面的直接入口。

**验证**：问一个笔记里有明确答案的问题，打印文件名、标题和内容预览。

**你要能说**：这是普通 RAG 基线。之后加的改写、BM25、RRF 和精排都与它对比，而不是一开始把所有技术混在一起。

---

## 6. 加查询改写

**新建文件**：`backend/app/services/query_rewriter.py`

**只做什么**：将“刚才那个融合有什么好处”改写成独立、完整的检索问题；失败时直接返回原问题。

**函数边界**：`query_rewrite(query: str, llm) -> str`。它不接收、也不调用 `vector_retriever`；查询改写和检索应保持解耦。

**验证**：分别输入口语化问题和完整问题，打印原问题与改写结果。

**当前优化**：完整、独立的问题不再调用改写模型；只有包含“刚才、前面、它、那个”等前文指代词的问题才改写。这样可少一次模型网络调用；改写失败时仍回退原问题。

---

### 当前阶段的调用顺序

不新建 `retrieval_pipeline.py`，也不把查询改写写进 `vector_retriever.py`。调用方按顺序完成：

```text
原问题 → query_rewrite() → 改写查询
                         ├→ vector_retriever(改写查询)
                         └→ bm25_retriever(改写查询)
```

这样改写只执行一次；两个检索函数仍然只负责各自的检索。

---

## 7. 加 BM25 关键词检索

**新建文件**：`backend/app/services/bm25_retriever.py`

**只做什么**：使用 `pkuseg` 进行中文分词，使用 `bm25s` 建立内存索引；输入改写后的查询，返回关键词 Top-K 片段。

**验证**：问函数名、缩写、专业术语等精确关键词问题，打印 BM25 结果。

**你要能说**：向量检索擅长语义，BM25 擅长精确术语，二者互补。

---

## 8. RRF 融合两路召回

**新建文件**：`backend/app/services/rrf_fusion.py`

**只做什么**：新建 `backend/app/services/rrf_fusion.py`。它只接收“向量检索结果”和“BM25 检索结果”两个形参，用手写 RRF 按排名合并，并用 `chunk_id` 合并相同片段，返回融合候选。它不接收用户问题，也不调用查询改写、向量检索或 BM25。

函数边界：

```python
rrf_fusion(results_list, rrf_k=60)
```

其中 `results_list[0]` 是向量检索结果，`results_list[1]` 是 BM25 结果。

**验证**：同一个问题打印“向量结果、BM25 结果、RRF 结果”。

**你要能说**：向量分数和 BM25 分数尺度不同，不能直接相加；RRF 只看排名，对分数尺度不敏感。

---

## 9. Cross-Encoder 精排

**新建文件**：`backend/app/services/reranker.py`

**只做什么**：使用 `BAAI/bge-reranker-base` 对 RRF 后的少量候选做“改写后查询 + 片段”相关性打分，只保留最终 Top-N。

**验证**：打印融合前后候选顺序和精排分数。

**你要能说**：混合检索补召回，精排提排序；精排只能处理已召回候选，且会增加延迟，所以不对全库精排。

---

## 10. 基于资料生成回答

**新建文件**：`backend/app/services/chat_service.py`

**只做什么**：将**原问题**和第 9 步最终片段组成 Prompt，要求模型仅依据资料回答；资料不足时返回拒答；同时带回 `SourceChunk`。检索用改写问题，回答仍围绕用户的原问题。

**验证**：

1. 笔记内问题：回答下能看到正确来源。
2. 笔记外问题：返回“当前笔记库资料不足”。

**你要能说**：RAG 不能保证零幻觉，但“只依据上下文 + 引用 + 拒答”能降低无依据回答并支持核对。

---

## 11. 将能力变成 FastAPI 接口

**新建文件**：

- `backend/app/api/notes.py`
- `backend/app/api/chat.py`

**修改文件**：`backend/app/main.py`

**接口顺序**：

1. `POST /api/notes/import`：导入笔记。
2. `GET /api/notes`：获取笔记库列表。
3. `POST /api/chat`：返回回答、改写后的查询、来源和拒答标记。

**验证**：打开 `http://127.0.0.1:8000/docs`，不依赖前端也能完成导入与问答。

**你要能说**：`api` 类似 Spring 的 Controller；`schemas` 类似 DTO；`services` 是业务层；`main.py` 只负责注册路由和启动。

---

## 12. Codex 接管前端

**Codex 修改**：`frontend/app.py` 与前端相关文件。

**页面功能**：

1. 笔记导入和处理状态。
2. 笔记列表。
3. 提问、加载状态、回答卡片。
4. 来源文件、章节和原文片段展开。
5. 资料不足提示。

**你的验收**：只需要按“导入 → 提问 → 看来源 → 问笔记外问题”体验，并反馈哪里不好用。

---

## 12.1 统一 RAG 编排与流式问答

**新建文件**：`backend/app/services/rag_service.py`

**只做什么**：这是各模块的统一调用方，不是新的检索器。它按以下顺序传参和调用已有函数：

```text
原问题
→ query_rewrite(原问题, LLM)
→ vector_retriever(改写查询, Chroma)
→ bm25_retriever(改写查询, Chunk 列表)
→ rrf_fusion([向量结果, BM25 结果])
→ reranker(改写查询, RRF 结果, Cross-Encoder)
→ chat_service(原问题, LLM, 最终片段)
```

**接口文件**：`backend/app/api/chat.py`。它不执行 RAG 业务逻辑，只接收 `ChatRequest`，调用 `rag_service.py`，通过 SSE 把“来源、文本片段、完成/错误状态”逐段返回前端。

---

## 12.2 当前性能优化与取舍

以下是实际代码已经做的优化；尚未跑基准测试，因此不能声称具体提速百分比。

| 优化点 | 原来会发生什么 | 现在怎么做 | 取舍 |
| --- | --- | --- | --- |
| 查询改写按需执行 | 每个问题都多一次 LLM 调用 | 仅含前文指代的口语化问题改写 | 极少数隐含上下文的问题可能不改写 |
| 笔记 Chunk 缓存 | 每次提问都重新读 Markdown、标题切分 | `load_all_chunks()` 使用缓存；导入新笔记后主动清除 | 只有导入接口会触发缓存失效 |
| BM25 延迟重建 | 不应在每次问答都建立索引 | 首次问答或导入新笔记后的下一次问答才重建 | 新导入后的第一问会稍慢 |
| 缩小候选集 | 两路各取 Top-10，精排 Top-4 | 两路各取 Top-6，精排 Top-3 | 极少数排在 7~10 名的资料不会进入精排 |
| 控制回答长度 | 回答可能生成较长文本 | Prompt 默认要求 3~5 个要点 | 需要长答案时可后续增加“展开回答”开关 |
| 前端等待反馈 | 检索阶段页面看起来像卡住 | 立即显示“正在理解问题并检索笔记”，拿到资料后变为“正在生成回答” | 当前不是后端逐阶段真实进度条 |
| 对话区固定滚动 | 新消息可能出现在输入框下方 | 历史消息放入固定高度滚动容器，输入框始终在下方 | 记录仅保存在当前 Streamlit 会话中 |

---

## 13. 最小验证与 README

**新建文件**：

- `docs/验证记录.md`
- `docs/演示稿.md`

**只做什么**：准备 10 个已知答案问题，对比纯向量检索与混合检索+精排时正确来源是否进入 Top-3；记录失败例子。

**不做什么**：不做 RAGAS 平台，不编造准确率，不宣称生产级性能。

---

## 14. 有时间再做：小测与单 Agent

**后续文件**：`backend/app/services/quiz_service.py`、`backend/app/services/agent_router.py`

- 小测：基于当前章节片段生成 3 道题并附来源。
- 单 Agent：只在“笔记问答”和“章节小测”两个工具间路由。

只有前 13 步稳定后才做；它们不是项目成立的前提。

## 当前开发位置

已完成：笔记读取、标题切分、配置、日志、Chroma 入库、向量检索、查询改写、BM25、RRF、Cross-Encoder、流式回答、FastAPI 笔记/聊天接口，以及 Streamlit 前端。

现在做：联调“上传 Markdown → 首次问答 → 查看来源 → 连续多轮提问”，记录真实延迟和失败样例；随后再决定是否继续做小测。
# 双模式检索（当前版本）

## 为什么要分模式

纯向量检索是已经验证过的 RAG 基线，速度更快；混合检索与精排更适合精确术语，但会增加模型与本地推理开销。因此不再把完整链路强加给每一次提问，而是由用户在页面主动选择。

## 新增步骤：模式参数与分流

1. 在 `schemas/chat.py` 的 `ChatRequest` 增加 `mode: Literal["fast", "accurate"]`，默认 `fast`。
2. 前端将当前模式随 `query` 一起传给 `POST /api/chat`。
3. `rag_service.py` 根据 `mode` 分流：
   - `fast`：原问题 → 向量检索 Top-3 → 生成回答。
   - `accurate`：原问题 →（按需）查询改写 → 向量 + BM25 → RRF → Cross-Encoder → 生成回答。
4. 两条链路都要先判断候选是否足够相关；不相关时返回正常的资料不足提示。

**验收：** 对同一问题分别选择两种模式；快速模式不产生改写、BM25、RRF、精排日志，精确查找则完整走完上述链路。
