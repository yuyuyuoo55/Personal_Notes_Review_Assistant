# 依赖统一由 requirements.txt 管理（适用于 Streamlit Community Cloud / Hugging Face Spaces）。
# 这里只保留 pytest 配置，避免 Streamlit 走 uv 读 [project] dependencies 时遇到历史 TOML 问题。
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.0,<10",
]
