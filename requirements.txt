# 个人笔记复习助手 · 云端部署依赖（Streamlit Community Cloud / Hugging Face Spaces）
# 说明：
#   - 以「快速模式（Agent 检索）」为线上主力，保证安装快、冷启动稳定；
#   - pkuseg 与 sentence-transformers/torch 已从代码改为可选：缺省时精确模式自动
#     降级为「查询改写 + 向量/BM25 双路召回 + RRF 融合（无精排）」，不阻塞运行。
#     如需完整精确模式（含精排），可自行加回下面两行（较大、冷启动慢）：
#         sentence-transformers>=5.2,<6
#         torch>=2.9,<3

streamlit>=1.60,<1.61
fastapi>=0.141,<0.142
uvicorn[standard]>=0.52,<0.53
httpx>=0.28,<0.29
python-dotenv>=1.2,<2
pydantic>=2.12,<3
python-multipart>=0.0.32

# RAG / 向量 / 检索
langchain>=1.3,<2
langchain-chroma>=1.1.0
langchain-community>=0.4,<1
langchain-deepseek>=1.0,<2
langchain-text-splitters>=1.1,<2
chromadb>=1.5,<2
dashscope>=1.25,<2
bm25s>=0.2,<0.3
