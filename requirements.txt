# 个人笔记复习助手 · 云端部署依赖（从 pyproject.toml 提取，供 Streamlit Community Cloud / Hugging Face Spaces 安装）
# 注意：torch + sentence-transformers 较大，首次冷启动偏慢；精确模式依赖它们。
# 部署平台用 Streamlit 时，下面这些包会自动由平台按 requirements.txt 安装。

streamlit>=1.60,<1.61
fastapi>=0.141,<0.142
uvicorn[standard]>=0.52,<0.53
httpx>=0.28,<0.29
python-dotenv>=1.2,<2
pydantic>=2.12,<3
python-multipart>=0.0.32

# RAG / 向量 / 检索
langchain>=1.3,<2
langchain-chroma>=1.1.0
langchain-community>=0.4,<1
langchain-deepseek>=1.0,<2
langchain-text-splitters>=1.1,<2
chromadb>=1.5,<2
dashscope>=1.25,<2
bm25s>=0.2,<0.3
pkuseg==0.0.25

# 精排（精确模式）：较大，可在精简部署时移除，届时精确模式会降级
sentence-transformers>=5.2,<6
torch>=2.9,<3
