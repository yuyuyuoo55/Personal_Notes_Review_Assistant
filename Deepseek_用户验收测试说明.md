# 个人笔记复习助手：Deepseek 用户验收测试说明

> 你的身份：**用户测试者 / 验收者**。请通过网页实际使用项目、记录问题；除非小余明确授权，不要修改项目代码、依赖、配置、模型缓存或已导入的笔记。

## 1. 项目目标

这是一个本地运行的个人笔记复习助手。用户导入 Markdown 学习笔记后，可以选择两种问答模式：

| 模式 | 名称 | 适用场景 | 后端链路 |
| --- | --- | --- | --- |
| 快速模式 | Agentic RAG | 日常复习、基础问答、需要承接上一轮对话的追问 | Agent 判断 → 直接回答 / 向量检索 Top-2 → 回答 |
| 精确查找 | Step RAG | 术语、命令、需要更可靠来源的知识问题 | 查询改写 → 向量 + BM25 → RRF → Cross-Encoder → 回答 |

## 2. 项目位置与启动方式

项目目录：

```text
D:\codex\project\vibe-coding\Personal_Notes_Review_Assistant
```

### 启动

在项目根目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\start_all.ps1
```

启动成功后打开：

```text
http://127.0.0.1:8501
```

后端接口文档（仅在需要排查时查看）：

```text
http://127.0.0.1:8000/docs
```

不要在测试报告中记录或展示 `.env` 中的 API Key。

## 3. 使用流程

1. 打开网页。
2. 在左侧选择一个 `.md` 文件并点击“导入到笔记库”。
3. 确认左侧“笔记库”和顶部“当前检索范围”显示导入结果。
4. 在中间选择“快速模式”或“精确查找”。
5. 输入问题，等待流式回答完成。
6. 查看回答、来源折叠面板和“本次回答耗时”。

## 4. 必测功能与预期结果

### A. 笔记导入

| 测试操作 | 预期结果 |
| --- | --- |
| 第一次导入一个 Markdown 文件 | 显示“已导入 文件名 · N 个片段”；上传框自动清空；左侧笔记库出现该文件 |
| 再次选择相同文件 | 显示“文件已在笔记库中，无需重复导入”；导入按钮不可点 |
| 刷新网页 | 已导入的文件和片段数量仍显示，不应变成 0 |
| 上传非 `.md` 文件 | 应被前端限制或后端拒绝，不应导致页面报错 |

建议测试资料：至少导入一份包含明确知识内容的笔记，例如 Git、Docker、Linux、Vue 等 Markdown 笔记。

### B. 快速模式（Agentic RAG）

#### B1. 基础闲聊不检索

依次输入：

```text
你好，我叫小余。
我叫什么名字？
```

预期：第二句能回答“小余”。这说明快速模式读取了本次会话的短期记忆。

#### B2. 基于笔记的知识问答

若导入了 Git 笔记，输入：

```text
介绍一下 Git 的常用命令。
```

预期：

- 能给出 Git 命令相关回答；
- 回答下方可以展开“参考笔记”；
- 不应出现 `DSML`、`tool_calls`、`SearchPersonalNotes` 等内部标记；
- 一般比精确查找更快。

#### B3. 会话记忆管理

快速模式仅使用短期内存：

- 最近 3 轮对话保留原文；
- 更早内容被压缩成摘要；
- 重启后端或新建浏览器会话后，记忆会清空；
- 精确查找不使用这份记忆。

可测试追问：

```text
先问：Git 的分支有什么用？
再问：刚才那个怎么创建？
```

预期：Agent 能将“刚才那个”理解为 Git 分支；若无法理解，也应正常追问或说明范围，不应报内部错误。

### C. 精确查找（Step RAG）

选择“精确查找”，观察页面链路提示是否为：

```text
原问题 → 查询改写 → 向量 + BM25 → RRF → Cross-Encoder → 回答
```

使用笔记中明确存在的问题，例如：

```text
介绍一下 Git 的常用命令。
```

预期：

- 正常返回回答和来源；
- 回答通常比快速模式慢；
- 首次加载精排模型时，页面会显示模型准备 / 下载阶段提示；已缓存模型时显示本机缓存加载提示；
- 不应该把模型、异常堆栈、`chunk_id` 等内部报错展示给用户。

### D. 资料不足场景

在只有“Docker 中部署 MySQL”资料、没有“MySQL 基础概念”资料时，输入：

```text
介绍一下 MySQL。
```

预期回答顺序：

1. 第一行明确写“当前笔记库资料不足。”
2. 说明缺少哪部分核心资料，例如 MySQL 概念或架构。
3. 说明实际检索到的内容，例如 Docker 部署 MySQL。

不应先输出一大段 Docker 部署细节，再在结尾才说资料不足。

注意：资料只要能覆盖问题的核心答案，即使不够百科全面，也应正常回答。例如笔记包含 Git 常用命令时，“介绍 Git 常用命令”不应误判为资料不足。

### E. 模式切换与历史记录

先在一种模式中完成至少一轮问答，再切换模式。

预期：聊天记录中出现模式分隔线，例如：

```text
已切换到：精确查找（Step RAG）
```

或：

```text
已切换到：快速模式（Agentic RAG）
```

两种模式共用展示历史，但分隔线应让人能看懂每段问答属于哪种模式。

## 5. 当前设计边界（不是 Bug）

1. 只支持本地 Markdown 笔记，不支持 PDF、Word、网页导入。
2. 不会联网补充答案，只依据用户导入的资料回答。
3. 会话记忆是内存级，不会跨后端重启长期保存。
4. Cross-Encoder 首次下载依赖网络；若模型源网络不可用，精确查找可能提示模型准备失败。
5. 快速模式由 Agent 自主判断是否检索，因此基础闲聊可能不显示笔记来源；技术知识问题应检索笔记。
6. 当前没有用户登录、多用户隔离、云同步和长期聊天记录持久化。

## 6. Bug 反馈格式

请按下面格式提交，避免只写“有 Bug”：

```markdown
### 问题标题
一句话描述问题。

### 测试模式
快速模式 / 精确查找

### 前置条件
导入了哪些文件；是否刚刷新页面；是否首次使用精确查找。

### 操作步骤
1. ...
2. ...
3. ...

### 实际结果
页面实际显示什么；如有报错，请贴完整文字或截图。

### 预期结果
你认为应该怎样。

### 可复现性
每次出现 / 偶尔出现 / 仅首次出现。
```

## 7. 验收结论格式

请最终按功能分别标注：

```markdown
- 笔记导入：通过 / 不通过
- 刷新后笔记列表：通过 / 不通过
- 快速模式基础问答与短期记忆：通过 / 不通过
- 快速模式笔记检索与来源：通过 / 不通过
- 精确查找完整链路：通过 / 不通过
- 资料不足提示：通过 / 不通过
- 模式切换分隔线：通过 / 不通过
- 页面交互与异常提示：通过 / 不通过
```

若不通过，请附上第 6 节的 Bug 反馈信息。
