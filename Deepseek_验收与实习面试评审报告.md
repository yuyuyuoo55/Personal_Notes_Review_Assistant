# 个人笔记复习助手 · 验收与实习面试评审报告（给 Codex）

> 生成时间：2026-08-22
> 测试对象：`D:\codex\project\vibe-coding\Personal_Notes_Review_Assistant`
> 本报告基于**真实运行测试**得出，不是读代码臆断。请 Codex 优先修复"第 2 节 问题清单"中的功能问题，其余为体验优化与面试素材。

---

## 0. 测试方式与范围（请先读这段，避免误读结论）

| 项目 | 说明 |
| --- | --- |
| 运行实例 | 用户已启动的前后端实例（FastAPI `127.0.0.1:8000`，Streamlit `127.0.0.1:8501`），测试全程**未重启、未修改**该实例 |
| 笔记库现状 | `data/uploads` 已有 6 篇：Git.md(10 片段)、Docker.md(19)、Linux.md(18)、Maven高级.md(8)、Vue.md(1)、vue小结.md(1)，Chroma 与 BM25 索引均在 `data/` 落盘 |
| 模型 | 聊天：DeepSeek deepseek-chat；Embedding：DashScope text-embedding-v4；精排：BAAI/bge-reranker-base（**本机已缓存**，未触发首次下载） |
| 测试手段 | 直接调用 HTTP API（SSE 全量解析）+ Streamlit `AppTest`（页面级验证：笔记列表渲染、模式切换分隔线）+ 源码交叉审查 |
| 约束遵守 | 未修改任何代码、依赖、`.env`、模型缓存；**未新增导入**任何笔记（避免污染真实笔记库）；未暴露任何 API Key |
| 诚实声明 | 页面级交互用 AppTest 与代码审查验证，未做真人浏览器自动化；"首次下载精排模型"的前端提示因模型已缓存，仅代码验证 |

---

## 1. 验收结论

- 笔记导入：**通过**（已导入 6 篇且片段数正确；重复导入返回 409"同名笔记已存在；当前版本不重复导入"；非 `.md` 返回 400"当前只支持上传 .md 格式笔记"；空文件返回 400"上传文件不能为空"；前端对重复文件显示"已在笔记库中"且禁用导入按钮、成功后清空上传框，代码验证）
- 刷新后笔记列表：**通过**（全新页面加载后侧边栏仍显示全部笔记与片段数；数据落盘于 `data/uploads` + `data/chroma/chroma.sqlite3` + `data/bm25/notes.bm25`，前端每次加载都重新请求 `/api/notes`）
- 快速模式基础问答与短期记忆：**通过**（"你好，我叫小余。"1.1s 直接回答不检索；"我叫什么名字？"0.9s 正确答出"小余"；"刚才那个怎么创建？"能正确把指代理解为"Git 分支"）
- 快速模式笔记检索与来源：**通过（附缺陷，见问题 #1/#2）**（主用例"介绍一下 Git 的常用命令"正确检索 Git.md 两个片段并给出带来源回答；但"Git 的分支有什么用？"存在稳定复现的召回失败 → 误判"没有对应片段"）
- 精确查找完整链路：**通过**（阶段提示 25% 缓存加载精排模型 → 70% 完整检索；`meta.rewritten_query`、3 个来源、回答合理；"它和加权融合有什么区别？"正确改写为"加权融合 区别"）
- 资料不足提示：**通过（精确查找完全符合规范；快速模式措辞不一致，见问题 #3）**（"介绍一下 MySQL"精确查找第一行即"当前笔记库资料不足。"，随后说明缺什么、实际检索到什么；"它和加权融合有什么区别"同理。快速模式能表达资料不足但措辞不同）
- 模式切换分隔线：**通过**（切换后出现"已切换到：快速模式（Agentic RAG）"/"已切换到：精确查找（Step RAG）"；无历史时不插入；同模式重复点击不重复插入）
- 页面交互与异常提示：**通过**（所有回答均未出现 `DSML`、`tool_calls`、`SearchPersonalNotes`、`chunk_id`、异常堆栈等内部内容；导入/问答异常前端均有友好提示）

**总体：功能可用，达到可验收标准；但有 2 个快速模式的功能缺陷建议修复后复测（见下）。**

---

## 2. 发现的问题

### 阻塞使用
无。

### 功能问题

#### 问题 #1【功能 · 建议修复】快速模式对"章节内嵌小节"类问题召回失败，误判"没有对应片段"，且答案与笔记事实矛盾
- **测试模式**：快速模式
- **前置条件**：已导入 Git.md（含 `## Git的常用命令 → ### 远程仓库操作 → #### 分支操作` 完整章节）
- **操作步骤**：1. 选择快速模式；2. 输入"Git 的分支有什么用？"
- **实际结果**：返回 2 个来源（均为"什么是Git / Git的简介"无关片段），回答"根据你的笔记，目前没有找到关于 Git 分支（branch）的具体说明…没有找到对应片段"。**但 Git.md 第 162–188 行明确有"分支操作"章节**（分支作用、`git branch 分支名` 创建、`git checkout` 切换、`git merge` 合并、`git branch -d` 删除）。
- **预期结果**：应检索到"远程仓库操作"片段（其中内嵌分支操作子节）并基于笔记回答。
- **可复现性**：每次出现（已复现 2 次，第 2 次独立会话仍复现）
- **根因（已用向量排序验证）**：对查询"Git 的分支有什么用？"，向量检索 top-6 排序为：①Git简介(0.80) ②什么是Git(0.82) ③**远程仓库操作(0.86，内含分支内容)** ④Git的作用(0.86) ⑤本地仓库操作(0.98) ⑥获取git仓库(1.06)。快速模式只取 **top_k=2** 且阈值 1.10 放行，含分支内容的片段排第 3 被漏掉 → Agent 基于两个无关片段误判资料不足。
- **连带影响**：同一会话继续问"刚才那个怎么创建？"时，Agent 继承上一轮错误结论（"笔记没有分支内容"），**绕过检索**（sources=0）直接用模型常识回答 `git switch -c` 等**笔记中不存在的命令**，与"只依据笔记回答、不编造"的设计矛盾（见问题 #2）。

#### 问题 #2【功能 · 建议修复】快速模式指代追问答对指代，但未检索笔记、输出笔记外内容
- **测试模式**：快速模式
- **前置条件**：同一会话先问"Git 的分支有什么用？"（即问题 #1 场景）
- **操作步骤**：接着输入"刚才那个怎么创建？"
- **实际结果**：正确理解"刚才那个"= Git 分支（记忆 ✓），但 sources=0（未调用检索），回答"你的笔记里目前还没有关于 Git 创建分支的具体命令"，随后给出 `git branch 分支名`、`git checkout -b`、`git switch -c` 等通用命令——**笔记里实际有"git branch (name) 创建分支名"**，且 `git switch -c` 并非来自笔记，属于模型常识补答。
- **预期结果**：应检索笔记后回答"git branch 分支名 创建分支、git checkout 切换、git merge 合并"并显示来源；至少不应声称"笔记里没有"。
- **可复现性**：每次出现（依赖问题 #1 先发生）
- **建议**：Agent 决策层把"上一轮检索结论"也纳入上下文，或对指代类问题强制走检索。

#### 问题 #3【功能/规范 · 建议统一】两种模式的"资料不足"措辞不一致
- 精确查找（`chat_service.py` 提示词强制）：第一行必须是"当前笔记库资料不足。"，之后按"原因 → 已检索到的资料"说明，实测完全符合验收说明 D 节要求。
- 快速模式（`agent_service.py` 的 ANSWER_SYSTEM_PROMPT）：只要求"明确回复'没有找到对应片段'"，实测回答为"根据您的笔记，我没有找到专门针对'MySQL 介绍'的完整说明…"，**第一行没有"当前笔记库资料不足。"**。
- 验收说明 D 节要求第一行明确写"当前笔记库资料不足。"。建议快速模式统一为同一措辞规范，否则面试时容易被追问"为什么两种模式行为不一致"。

### 体验问题

#### 问题 #4【体验】快速模式"来源面板与回答矛盾"
"Git 的分支有什么用？"时，来源面板显示了 Git简介 片段，但回答却说"没有找到对应片段"。用户会困惑"既然显示了参考笔记，为什么说没有"。建议：当 Agent 判定资料不足时，前端不再展示这些无关来源，或来源与"资料不足"提示同时出现并明确说明"以下片段与问题不相关"。

#### 问题 #5【体验】输入框占位符问题在当前笔记库必然触发"资料不足"
`frontend/app.py` 占位符为"例如：RRF 和加权融合有什么区别？"，但当前笔记库（及仓库提供的测试笔记）**没有任何 RRF/加权融合内容**，用户照着示例提问会得到"资料不足"。建议换成库内能回答的示例（如"Git 有哪些常用命令？"）。

#### 问题 #6【体验/安全边界】SSE `meta` 事件把 `chunk_id` 发给浏览器
`rag_service.build_source_chunks()` 构造的 sources 包含 `chunk_id` 字段，前端未渲染（页面无泄漏 ✓），但浏览器网络层可见。若要满足"绝不暴露 chunk_id"的严格口径，后端可不返回该字段。

### 建议优化（未复现为 Bug）

- **优化 #7**：快速模式 top_k=2 对长/复合笔记召回不足，可考虑 top_k=3~4 或加 BM25 兜底（问题 #1 已实证）。
- **优化 #8**：`MarkdownHeaderTextSplitter` 只按 #/##/### 切分，"#### 分支操作"被并入"### 远程仓库操作"片段，来源标题显示"远程仓库操作"而非"分支操作"，且片段过大。可把 `####` 也纳入切分或按长度二次切分。
- **优化 #9**：精确查找"介绍一下 MySQL"时召回了 Linux.md 的"Linux介绍"片段（明显无关），精排后未过滤低分片段。可加"精排分数最低阈值"或至少不出现在来源面板。
- **优化 #10**：`data/bm25/` 下有 `bm25_test.bm25`、`verification_notes.bm25` 等测试产物，建议清理或移出正式数据目录，面试讲解时避免混淆。

### 未复现（明确写出来）

- 所有模式、所有用例均**未出现** `DSML`、`tool_calls`、`SearchPersonalNotes`、`chunk_id`、异常堆栈泄漏（正则兜底转换 DSML 生效）。
- 未出现"资料足够却误判资料不足"的相反错误（"介绍 Git 常用命令"两模式均正常回答）。
- 未出现导入后页面报错、笔记列表归零、模式切换异常。

---

## 3. 面试可讲的项目事实（全部实测验证）

1. **解决什么用户问题**：学生/开发者的本地 Markdown 学习笔记无法被"提问式复习"。导入笔记后，用自然语言提问，系统只依据笔记回答并给出可回看的来源片段。
2. **两种模式的取舍**：
   - 快速模式（Agentic RAG）：日常复习、闲聊、承接上一轮追问。由 LLM 自主决定是否调用检索工具，只做轻量向量检索（top-2）。**快（预热后约 1–3s），但召回弱**（实测"Git 分支"漏召回）。
   - 精确查找（Step RAG）：术语、命令、需要可靠来源的问题。固定六步链路（原问题 → 查询改写 → 向量+BM25 双路召回 → RRF → Cross-Encoder 精排 → 生成），**约 6–9s，可解释、可复现**（实测能命中快速模式漏掉的"分支操作"内容）。
3. **为什么快速模式属于 Agentic RAG**：`decide_agent_action` 用 `bind_tools([SearchPersonalNotes])` 让 LLM 决策"直接回答 or 检索"，检索结果作为 ToolMessage 交回模型生成；并兼容了 DeepSeek 返回 DSML 文本（`<invoke name="SearchPersonalNotes">`）时的正则转标准 tool_call 处理。实测问候不检索、技术问题检索、DSML 无泄漏。
4. **为什么精确查找属于 Step RAG**：没有 Agent 决策，固定执行"查询改写 → 双路召回 → RRF → 精排 → 生成"，每步独立可测、可解释（前端有链路提示 + 进度阶段事件）。
5. **各组件作用（实测）**：
   - 查询改写：仅当问题含"它/刚才/那个/上面"等指代标记才调用 LLM 补全（实测"它和加权融合有什么区别？"→"加权融合 区别"）；完整问题跳过，省一次调用。
   - 向量检索：DashScope embedding + Chroma（L2 距离，阈值 1.10），负责语义召回。
   - BM25：关键词召回，索引落盘 `data/bm25/notes.bm25`，导入新笔记后置脏标记、下次提问才重建（惰性）。
   - RRF：按两路排名融合 1/(k+rank)，避免不同分数尺度直接相加。
   - Cross-Encoder：BAAI/bge-reranker-base，对 RRF 结果精排取 top-3；首次使用下载到本机缓存（本机已缓存，实测提示"正在从本机缓存加载精排模型…"）。
   - 资料不足处理：阈值 + 关键词命中判断，精确模式第一行强制"当前笔记库资料不足。"（实测 MySQL、加权融合两个场景均正确）。
   - 短期记忆：快速模式专用，最近 3 轮原文 + 更早内容异步压缩为摘要（≤1600 字），内存级；实测"我叫什么名字？""刚才那个"均正确。
6. **当前项目边界**（面试要主动讲清，不夸大）：仅本地 `.md`；不联网补充答案；会话记忆重启即清空；Cross-Encoder 首次下载依赖网络；无登录、无多用户隔离、无云同步、无聊天记录持久化；无自动化评测集与量化指标。

---

## 4. 可能的实习面试追问（10 个，附真实答案方向，不编造指标）

1. **快速模式和精确查找在工程实现上到底差在哪？** → 一个用 Agent 决策（bind_tools + ToolMessage 回填），一个固定流水线。可以主动讲实测案例：同一问题"Git 的分支有什么用？"快速模式漏召回、精确模式命中，说明召回深度差异。
2. **RRF 为什么不用分数加权？** → 向量距离与 BM25 分数尺度不同不可直接相加；RRF 只用排名，`1/(k+rank)`，实测实现两路各取 top-6 融合。
3. **向量检索为什么设 1.10 距离阈值？** → 避免把明显无关片段交给模型编答；实测"介绍一下 MySQL"场景正是靠它触发资料不足。
4. **Cross-Encoder 和 Embedding 检索有什么区别？** → Embedding 是单向量相似度（快、可索引），Cross-Encoder 是 query+doc 成对打分（准、慢），所以放在 RRF 之后只对少量候选精排取 top-3。
5. **"资料不足"怎么判断、怎么兜底？** → 阈值 + BM25 关键词命中 → 精确模式第一行"当前笔记库资料不足。"+ 说明缺什么/检索到什么；可主动承认两种模式措辞当前不一致（问题 #3）以及改进方案。
6. **会话记忆怎么实现的？** → 内存字典按 conversation_id 存"最近 3 轮原文 + 更早摘要"，异步线程压缩不阻塞回答；只喂给快速模式。可讲清为什么不做持久化（本地单机 MVP、重启清空是设计边界）。
7. **Agent 工具调用如果模型返回的不是标准格式怎么办？** → DeepSeek 有时把工具调用输出成 DSML 文本，用正则转成标准 AIMessage.tool_calls 再走 LangChain 工具循环；实测页面无任何工具调用痕迹泄漏。
8. **为什么 BM25 索引要"惰性重建"？** → 导入笔记后置脏标记，下次提问才重建，避免每次提问都重建索引的开销；回答不上来时可以承认这是简单方案，可换增量更新。
9. **你的项目效果怎么验证？** → 目前是手工验收用例（就是这份报告）；诚实说明**没有自动化评测集和量化指标**，这正是下一步要补的（见第 5 节）。
10. **多用户/并发怎么办？** → 没做；单机单用户、内存态、无鉴权。回答要强调"我知道边界在哪"，不要包装成生产系统。

---

## 5. 实习项目评价

- **当前是否达到"可以写进 AI/RAG 实习简历"的程度**：**达到基本线**。理由：①两种真实可运行的 RAG 范式（Agentic RAG / Step RAG）且能讲清取舍；②链路完整（改写/双路召回/RRF/精排/资料不足/记忆）且每步有落盘与日志；③有真实使用证据（这份验收报告）。**但属于"本地单机 MVP"，表述必须诚实。**
- **最值得补的 3 个验证或能力**（按优先级）：
  1. **自动化评测**：构造 20~30 条 QA 用例（覆盖两模式、资料不足、指代追问），统计检索命中率与"回答可被来源支撑"的比例，把"我测过"变成"有指标"。这是 RAG 实习面试最容易被问倒的点。
  2. **修复快速模式召回不足并补回归用例**：把"Git 的分支有什么用？"做成回归测试（当前必挂），修 top_k/多路召回（问题 #1/#2）。
  3. **任意一个"状态工程"能力**：会话记忆持久化（如 SQLite 会话表）或多用户隔离（会话隔离），证明不只是内存玩具。
- **哪些表述不能写进简历（避免夸大）**：生产级、高并发、分布式部署、线上用户量、准确率提升百分比、模型训练/微调、多租户、云同步——**这些项目都没有实测完成**；简历只写"本地单机、可复现的 RAG 双模式实现 + 手工验收"。

---

## 附：实测证据速查

### 耗时对比（本机实测，单次）

| 场景 | 模式 | 耗时 | 来源数 |
| --- | --- | --- | --- |
| 你好，我叫小余。 | 快速 | 1.1s | 0 |
| 我叫什么名字？ | 快速 | 0.9s | 0 |
| 介绍一下 Git 的常用命令 | 快速 | 22.6s（冷启动，预热后 1–3s） | 2（Git.md） |
| Git 的分支有什么用？ | 快速 | 3.2s / 2.3s（复现） | 2（均为无关简介片段） |
| 刚才那个怎么创建？ | 快速 | 2.1s | 0 |
| 介绍一下 MySQL | 快速 | 3.5s | 2（Docker部署mysql、Linux介绍） |
| 介绍一下 Git 的常用命令 | 精确 | 6.9s | 3（本地/远程仓库操作、什么是Git） |
| Git 的分支有什么用 | 精确 | 6.1s | 3（含"远程仓库操作"→内含分支内容） |
| 介绍一下 MySQL | 精确 | 6.9s | 3（Docker部署mysql、Docker网络、Linux介绍） |
| 它和加权融合有什么区别？ | 精确 | 8.6s | 3（改写为"加权融合 区别"） |

### 关键行为摘要（均来自实际 SSE 输出）
- 快速模式问候：`sources=0`，直接回答，无检索痕迹。
- 快速模式 Git 命令：`sources=2`（Git.md · 本地仓库操作 / Git的简介），回答正确且带来源。
- 精确查找阶段事件：`[stage] 25% 正在从本机缓存加载精排模型…` → `[stage] 70% 精排模型已就绪，正在执行完整检索…`。
- 精确查找资料不足（MySQL）：回答第一行"当前笔记库资料不足。"，随后"资料不足的原因…已检索到的资料…"。
- 所有用例 leak-check：`无内部内容泄漏`。

### 测试后仓库状态
- 未修改任何代码/依赖/`.env`/模型缓存/已导入笔记；未新增导入。
- 测试临时脚本已删除（`zz_test_tmp/`），仓库保持测试前原状。
- 会话记忆为内存态，后端重启后自动清空，无残留。
