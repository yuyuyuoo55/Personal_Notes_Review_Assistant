# 提示词：笔记复习助手 UI 重构（多页 + 顶部导航 + 会话内保存 Key）

> 用法：把本文件交给 codex。**设计图已确认（见下），现在按图实现生产代码，不要重新出设计。**
> 目标：把当前的「单页大杂烩 Demo」重构为「多页、有导航栏、Key 会话内记忆、页面分工清晰」的产品化界面。
>
> **已确认的 4 页设计**：智能问答(首页) / 知识库(文档) / 设置 / 关于。顶部导航栏，风格沿用现有笔记感配色（米白底、墨绿主色、淡粉点缀）。

---

## 一、大方向（务必遵守）

1. **多页面结构**，用 Streamlit `st.navigation` 或顶部导航页签实现，**不要再把所有功能堆在一个页面**。
2. **顶部 or 侧边导航栏**：类似常见后台/应用，能点页面切换。参考截图那种「首页 / 智能问答 / 知识库 / ...」的横向导航。
3. **API Key 会话内记忆**：用户在「设置」页输入一次 Key，存到 `st.session_state`，**当前浏览器会话内**其他页面都用这个 Key，不用每个页面重复粘贴。
   - ⚠️ **安全**：Key 只存 `st.session_state`，**不写入数据库/日志/后端持久化**。刷新浏览器会话可能清空（可说明），不做账号系统。
4. **页面职责清晰**：
   - 问答页（首页）：聊天/提问、模式切换、来源展示。
   - 文档库页：导入(上传 .md/图片)、笔记列表、删除/清空。
   - 设置页：只放 API Key 输入框（及其他少量配置，可预留扩展）。默认值不要都暴露，保持简洁。
   - 关于页：项目简介、GitHub/技术栈/致谢。

---

## 二、页面结构（4 页）

### 1. 问答页「首页 / 智能问答」
- 顶部导航栏选中态。
- 主区：聊天窗口、问题输入框、快速/精确模式切换、（可选）图片上传问答。
- 显示：回答、来源卡片（含图片展示）、耗时。
- **Key 如果没设置**：在页面顶部给个提示「请先在『设置』页填 DeepSeek API Key」，并禁用提问，引导去设置页。

### 2. 文档库页「知识库 / 文档」
- **导入区**：上传 .md、图片(.jpg/.png/.webp)，有导入按钮。
- **笔记列表**：列出已导入的笔记/图片，每条有「删除」按钮（带确认），提供「清空全部」。
- 显示每个笔记的片段数。

### 3. 设置页「设置」
- **API Key 输入框**（`type=password`），保存到 `st.session_state`。
- 保存后显示「已保存，当前会话内有效」。
- （可预留扩展）模型名、超时等默认值标注「后续可配」，**本页只放 Key，保持简洁**。
- 可能放一个「清除 Key」按钮（清掉 session 里的 Key）。

### 4. 关于页「关于」
- 项目名、一句话简介。
- 技术栈（FastAPI + Streamlit + LangChain + Chroma + BM25 + DeepSeek Vision）。
- GitHub 链接、在线 Demo 链接。
- 致谢/说明。

---

## 三、设计风格（参考现有）

- 保持现有清新、笔记感的配色（米白底、墨绿主色、淡粉点缀），不要突兀。
- 导航栏、卡片圆角、留白统一。
- Streamlit 组件用 `st.navigation` + `st.Page`（若用 Streamlit 1.36+），或用顶部页签。
- 若组件限制，可先用 `st.sidebar.radio` 或 `st.sidebar.page_link` 做页面切换，再逐步打磨成顶部导航。

---

## 四、实现要求（设计已确认，现在按图实现）

**请实现生产代码（改 `streamlit_demo.py` 及必要的文件），严格按设计图做 4 页，不要重新出设计。**

要求：
1. **多页面结构**：用 `st.navigation` + `st.Page`（Streamlit 1.36+）或侧边栏导航，实现「智能问答 / 知识库 / 设置 / 关于」4 页。
2. **顶部/侧边导航栏**：风格与设计图一致；主页选中态高亮。
3. **Key 会话内记忆**：在「设置」页输入 Key → 存 `st.session_state` 全局键（如 `deepseek_key`），问答页读取同一个键。**不要各页存一份互不通。**
   - 无 Key 时：问答页顶部提示「请先在设置页填 Key」，提问/上传禁用，可跳设置页。
4. **问答页**：聊天 + 快速/精确切换 + 来源卡片（含图片展示）+ 耗时。保留现有 RAG/图片/流式逻辑，只把 UI 挪进这个页。
5. **知识库页**：上传(.md/图片) + 笔记列表（类型标记 MD/PNG/JPG、片段数、时间、删除/确认、清空全部）。导入、删除逻辑复用现有。
6. **设置页**：仅 API Key 输入（password）+ 保存/清除 + 「仅会话内有效」提示。
7. **关于页**：项目简介、技术栈标签、GitHub/在线 Demo 链接。

**分步做、每步能跑**：先做导航 + 4 页骨架，再逐页填功能（问答→知识库→设置→关于），**每一步都确保 `python -c "import streamlit_demo"` 能过、页面能渲染**，不要一次改完再一次性调试。

实现完成后给出：4 页截图/预览 + 说明 Key 如何在页间共享。

---

## 五、注意事项 / 不要踩的坑

1. **不要破坏现有功能**：RAG 问答、图片导入、删除、图片压缩、Key 验证这些都是核心，重构 UI 时逻辑不能丢。
2. **Key 会话内记忆**：改后「问答页」要能在「设置页」填的 Key 可用（共享 session_state）。**不要**每个页面各存一份互相不通。
3. **导航切换后状态保留**：切页不应丢失已导入的笔记列表、session 里的 Key。
4. **性能**：重构后冷启动/路由切换别明显变慢（Streamlit 多页注意缓存/重复加载）。
5. **图片/文档导入入口**：挪到文档库页后，问答页专注问答，不要重复出现多余上传控件（除非明确要「问答时临时传图」）。
6. **兼容**：`streamlit_demo.py` 作为 Cloud 入口，重构后要能在 Streamlit Cloud 正常跑。
