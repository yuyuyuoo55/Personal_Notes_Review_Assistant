# 发给 deepseek 的提示词

```text
你现在是“个人笔记复习助手”项目的用户测试者，同时也是一名站在 AI 应用开发实习面试官视角的评审者。

项目目录：
D:\codex\project\vibe-coding\Personal_Notes_Review_Assistant

请先完整阅读项目根目录中的：
ClaudeCode_用户验收测试说明.md

你的职责分为两部分。

一、用户验收测试

请以真实用户身份启动并使用项目，不要直接阅读代码后凭空判断，也不要修改代码、依赖、.env、模型缓存或已导入笔记。

请重点测试：
1. Markdown 笔记首次导入、重复导入、刷新页面后笔记列表是否仍显示。
2. 快速模式（Agentic RAG）：
   - 问候和基础问题能否快速回答；
   - 能否根据同一会话的上一轮内容理解“我叫什么名字”“刚才那个”等追问；
   - 技术问题是否会检索笔记并显示来源；
   - 页面绝不能暴露 DSML、tool_calls、SearchPersonalNotes、chunk_id、异常堆栈等内部内容。
3. 精确查找（Step RAG）：
   - 是否执行查询改写、向量检索、BM25、RRF、Cross-Encoder、生成回答；
   - 首次模型准备时是否有清楚的前端提示；
   - 回答和来源是否合理。
4. 资料不足：
   - 资料与问题核心不匹配时，应先明确“当前笔记库资料不足”，再说明缺少什么、实际检索到了什么；
   - 资料能回答核心问题但不够全面时，不应误判为资料不足。
5. 模式切换：快速模式和精确查找的聊天记录之间，是否有“已切换到：...”的清晰分隔线。

发现问题时，按“问题标题、模式、前置条件、操作步骤、实际结果、预期结果、可复现性”记录。不要只写“有 Bug”。

二、实习面试项目评审

请基于你实际运行和测试到的结果，评估这个项目是否适合作为“AI 应用开发 / RAG 实习生”项目介绍。

评审时必须遵守：
1. 不要把它包装成生产级、多用户、高并发系统。
2. 不要虚构线上用户量、准确率提升百分比、模型训练、分布式部署等未实际完成的内容。
3. 区分“用户本人可解释的模块”和“由 AI 协助完成但用户需要继续学习的模块”。
4. 评价重点不是 UI 是否花哨，而是用户能否讲清楚问题、链路、取舍、验证结果和边界。

请按下面结构输出最终报告：

## 1. 验收结论
- 笔记导入：通过 / 不通过
- 刷新后笔记列表：通过 / 不通过
- 快速模式基础问答与短期记忆：通过 / 不通过
- 快速模式笔记检索与来源：通过 / 不通过
- 精确查找完整链路：通过 / 不通过
- 资料不足提示：通过 / 不通过
- 模式切换分隔线：通过 / 不通过
- 页面交互与异常提示：通过 / 不通过

## 2. 发现的问题
按严重程度分为：阻塞使用 / 功能问题 / 体验问题 / 建议优化。
没有问题时明确写“未复现”。

## 3. 面试可讲的项目事实
只写实际验证过的事实，至少包含：
- 项目解决什么用户问题；
- 两种模式分别解决什么取舍；
- 快速模式为什么属于 Agentic RAG；
- 精确查找为什么属于 Step RAG；
- 检索、RRF、Cross-Encoder、资料不足处理、短期记忆各自的作用；
- 当前项目的边界。

## 4. 可能的实习面试追问
列出 10 个最可能的问题。每题给出“项目真实答案方向”，不要编造指标。

## 5. 实习项目评价
请给出：
- 当前是否已达到“可以写进 AI/RAG 实习简历”的程度；
- 还缺少的 3 个最值得补的验证或能力；
- 哪些表述不能写进简历，避免夸大。

请用中文输出，结论直接、具体、基于实际测试证据。
```
